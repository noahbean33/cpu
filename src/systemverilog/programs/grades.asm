					################################################
					# grades_base = 0x00000000					   #
					################################################
0x0000: 			addi    x10, x0, 0          # x10 = grades base (0x0)

					################################################
					# Write grades to memory					   #
					################################################
0x0004: 			addi    x5,  x0, 28
0x0008: 			sb      x5,  0(x10)

0x000C:  			addi    x5,  x0, 34
0x0010: 			sb      x5,  1(x10)

0x0014:  			addi    x5,  x0, 43
0x0018: 			sb      x5,  2(x10)

0x001C:  			addi    x5,  x0, 50
0x0020: 			sb      x5,  3(x10)

0x0024: 			 addi    x5,  x0, 55
0x0028: 			 sb      x5,  4(x10)

					################################################
					# results_base = 0x00000010					   #
					################################################
0x002C:  			addi    x11, x0, 16         # x11 = results base (0x10)

					################################################
					# Setup grading loop 						   #
					################################################
0x0030: 			addi    x12, x0, 5          # number of grades
0x0034:				addi    x14, x0, 60         # pass threshold

					################################################
					# Grade processing loop					       #
					################################################
0x0038:	grade_loop:	lbu     x5,  0(x10)         # load grade

0x003C:				srli    x6,  x5, 2          # grade >> 2
0x0040:				add     x6,  x5, x6         # new_grade = g + (g >> 2)

0x0044:				slt     x7,  x6, x14        # new_grade < 60 ?
0x0048:				bne     x7,  x0, fail       # branch to fail

					################################################
					# Pass path									   #
					################################################
					
0x004C:  	pass:	addi    x8,  x0, 1
0x0050:  			sb      x8,  0(x11)
0x0054:  			jal     x0,  next

					################################################
					# Fail path									   #
					################################################
					
0x0058:	   fail:	sb      x0,  0(x11)

					################################################
					# Loop update								   #
					################################################
0x005C:    next:	addi    x10, x10, 1         # next grade
0x0060: 			addi    x11, x11, 1         # next result
0x0064: 			addi    x12, x12, -1
0x0068: 			bne     x12, x0, grade_loop

					################################################
					# End of program (halt)						   #
					################################################
0x006C: 			addi    x0, x0, 0             # nop (acts as halt here)


